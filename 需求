学生管理系统 —— 需求说明
========================
personal_accounting/
├── main.py                 # 入口：菜单循环，只负责流程
├── models.py               # 数据模型：Record / Income / Expense
├── storage.py              # 数据保存：JSON 读写
├── service.py              # 业务：新增、查询、统计、搜索、排序
├── data/
│   └── records.json        # 程序运行后自动生成
├── tests/
│   └── test_service.py     # pytest 测试
├── requirements.txt        # 依赖列表
├── .gitignore              # 忽略缓存、虚拟环境、数据文件
└── README.md               # 项目说明、运行方法、截图
一、项目目标
在命令行里运行、能真正日常使用的“学生管理系统”：
启动后进入菜单，可 添加、查询、修改、删除、展示、统计 学生；
学生数据存入文件，关闭程序再打开，数据不丢。
做完并跑顺后，用同一套思路换个业务再做一个程序，验证学到的是“方法”而不是“背代码”。

二、使用者与场景
使用者：老师 / 班长，单人、本机使用。
场景：记录一个班学生的学号、姓名、年龄、成绩；随时查询、改错、删人、看全班统计。

三、业务需求（功能）
0. 启动与主菜单
   - 启动打印欢迎语，进入主菜单循环。
   - 菜单项：1 添加学生 / 2 查询学生 / 3 修改学生 / 4 删除学生
             5 显示全部 / 6 班级统计 / 0 退出
   - 选 0 退出并保存；选其他无效项时提示“没有这个选项”，重新显示菜单，不崩溃。
1. 添加学生
   - 依次输入 学号 / 姓名 / 年龄 / 成绩，组成一个学生存入列表。
   - 学号不能重复：重复则提示并让用户重新输入。
   - 年龄必须是数字；成绩必须是 0–100 的数字。非法则提示原因并重新输入。
2. 查询学生
   - 按学号查找：找到打印该学生信息；找不到提示“查无此人”。
3. 修改学生
   - 按学号找到后，可修改其 姓名 / 学号 / 成绩；改完提示。
   - 年龄在添加学生时确定，后续不允许修改。
4. 删除学生
   - 按学号删除：删除后提示成功；删不存在的学号提示“查无此人”。
5. 显示全部
   - 以整齐的表格打印全部学生（学号 / 姓名 / 年龄 / 成绩），一行一个，方便阅读。
6. 班级统计
   - 输出：总人数、平均分、最高分（含对应姓名）、最低分（含对应姓名）。
7. 退出
   - 退出前自动把数据写回文件，并打印再见提示。

四、框架
1. 数据结构
   - 一个学生 = 一个字典：{“id”: 学号, “name”: 姓名, “age”: 年龄, “score”: 成绩}
   - 所有学生 = 一个列表 students；程序全程操作这个列表。
2. 数据保存（students.json，UTF-8）
   - 启动时自动读取；文件不存在或损坏时按“空列表”处理，程序不崩。
   - 每次 增 / 删 / 改 之后立即写回文件。
3. 代码结构
   - 初期集中在 main.py（入口 + 菜单 + 功能），用函数把每个功能隔开；
     等 main.py 变长难维护时再拆成：main.py / storage.py / service.py。

五、技术支持
- 语言与依赖：Python 3，只用标准库（json / os / pathlib 等），初期无需第三方库。
- 工具：PyCharm（或命令行）运行；Git 每完成一个功能提交一次。
- 测试（可选加分）：用 pytest 给 添加 / 查询 / 删除 / 统计 各写少量断言。
- 后续扩展（本期做完后）：
  1) 用同一套流程换个业务重做（如图书管理 / 记账）；
  2) 用 SQL / Pandas 对保存的数据做统计报表。

六、验收标准（一项功能做完、能手动走通，才算该项完成）
- python main.py 能启动并进入菜单，选 0 能干净退出。
- 手动走一遍全流程：添加 → 重复学号被拦 → 查询 → 修改 → 删除 → 显示 → 统计 → 退出，
  程序全程不崩溃、结果与输入一致。
- 退出后重新启动，上一轮添加的学生仍在（数据已成功落盘到 students.json）。
- 每次“能多做一个动作”都 git commit 一次，提交信息写清这一步做了什么。
